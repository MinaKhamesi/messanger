import { makeStyles } from '@material-ui/core/styles';


const useStyles = makeStyles({
  root: {
    height:200,
    display:'block',
    margin:'auto',
    padding:200,
  }
});

export default useStyles;